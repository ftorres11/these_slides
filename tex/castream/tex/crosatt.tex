%--------------------------------------------------------------------------------------------------
\begin{frame}[t]
    \frametitle{Cross Attention Stream}
    \small
    Given a model $f$ we consider locations where critical operations take place:
    \begin{equation}
        f = g \circ \Th{GAP} \circ f_L \circ \dots f_0
    \end{equation}\pause
    %\vspace{20pt}
    On another hand, a $[\Th{CLS}]$ token embedding is initialized, as  $\mathbf{q}_0 \in 
    \mathbb{R}^{d_0}$, updated with a secquence of embeddings $\mathbf{q}_\ell \in \mathbb{R}^{d_\ell}$
    via interaction along a stream with features on stage  $\ell$:
    \begin{equation}
        \mathbf{q}_{\ell+1} = W_\ell\cdot\Th{CA}_\ell (\mathbf{q}_\ell, F_\ell)
    \end{equation}
    \pause
    Visualized as:
    \input{fig/castream/tex/ca_mainfig.tex}
\end{frame}