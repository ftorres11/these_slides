\section[CA-Stream]{CA-Stream: Attention-based pooling for interpretable image recognition}
%--------------------------------------------------------------------------------------------------
\begin{frame}[t]
    \frametitle{Preliminaries}
    \framesubtitle{Premise}
    \begin{center}
        \emph{The \Th{[CLS]} token in transformers updates an abstract representation via 
        attention with patch embeddings across the depth of a model. Is it possible to 
        train a similar class representation on CNNs?}
        \vspace{20pt}
        \input{fig/castream/tex/ca_mainfig.tex}
    \end{center}
\end{frame}
%--------------------------------------------------------------------------------------------------
\begin{frame}[t]
    \frametitle{Preliminaries}
    \framesubtitle{Contributions}
    \begin{itemize}
        \item \textbf{CAM is Attention}\\
        CAM behaves in a similar manner as attention.
        \item \textbf{Cross Attention: [CLS] on CNNs}\\
        Training of a [CLS] token, alongside standard CNNs.
        \item \textbf{CAM on more architectures}\\
        CAM typical validation only found on ResNet and VGG. Extension to ConvNeXt.
    \end{itemize}
\end{frame}
%--------------------------------------------------------------------------------------------------
\input{tex/castream/tex/ca_cam}
\input{tex/castream/tex/crosatt}
\input{tex/castream/tex/exp_setup}
\input{tex/castream/tex/qualitative}
\input{tex/castream/tex/quantitative}
\input{tex/castream/tex/conclusion}